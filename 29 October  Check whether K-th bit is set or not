class Solution
{
    public:
    // Function to check if Kth bit is set or not.
    bool checkKthBit(int n, int k)
    {
        // Your code here
        // It can be a one liner logic!! Think of it!!
        long long kpop=pow(2,k);
        if((n^kpop)>=n){
            return 0;
        }
        else{
            return 1;
        }
    }
};
