class Solution{
public:
    void Reverse(stack<int> &St){
        
        vector<int>arr;
        
        while(!St.empty())
        {
            arr.push_back(St.top());
            St.pop();
        }
        
        for(auto i:arr)
        {
            St.push(i);
        }
        
    }
};
